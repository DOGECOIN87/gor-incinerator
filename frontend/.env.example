# Gor-Incinerator Frontend Environment Variables
# Copy this file to .env.local and fill in your values

# API Backend URL (for Gorbag Wallet integration mode)
# Leave empty to use direct wallet mode (current behavior)
VITE_API_BASE_URL=

# API Key (only needed if frontend calls backend directly)
# For Gorbag Wallet integration, the wallet handles API authentication
VITE_API_KEY=

# Fee Recipient Address (for direct mode only)
# Used when frontend builds transactions directly without backend
VITE_FEE_RECIPIENT=

# Gorbagana RPC URL (for direct mode only)
VITE_GOR_RPC_URL=https://rpc.gorbagana.com

# Mode: "direct" or "api"
# direct = Frontend builds transactions (current behavior)
# api = Frontend calls backend API (for Gorbag Wallet integration)
VITE_MODE=direct
